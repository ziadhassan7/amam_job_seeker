import 'package:flutter/material.dart';

class AppColor {
  static const Color primary = Colors.black;
  static const Color primaryLight = Colors.black54;
  static const Color background = Color.fromRGBO(244, 244, 244, 1);

  static const Color accent = Color.fromRGBO(123, 188, 0, 1);
  static const Color accentDark = Color.fromRGBO(34, 89, 21, 1.0);
}